// const, var, let
function batch_16(dataOne, dataTwo) {
  const sum = dataOne + dataTwo;
  return sum;
}

function two(a, b) {
  const result = batch_16(a, b);
  console.log(result);
}

two(10, 20);
